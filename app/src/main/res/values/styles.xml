<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@color/colorAppBackground</item>
    </style>

    <style name="SplashTheme" parent="@style/AppTheme">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="MainCategoryText" parent="@style/AppTheme">
        <item name="android:textSize">@dimen/base_text_size</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">@dimen/medium_padding</item>
    </style>

    <style name="DescriptionHeaders" parent="@style/AppTheme">
        <item name="android:textSize">@dimen/base_text_size</item>
        <item name="android:textColor">@color/colorDescriptionHeaders</item>
    </style>

    <style name="HistoryHeaders" parent="@style/AppTheme">
        <item name="android:textSize">@dimen/header_text_size</item>
        <item name="android:textColor">@color/colorDescriptionHeaders</item>
    </style>

    <style name="HistoryText" parent="@style/AppTheme">
        <item name="android:textSize">@dimen/base_text_size</item>
        <item name="android:paddingLeft">@dimen/expandableListChildPadding</item>
        <item name="android:textColor">@color/colorDescriptionText</item>
    </style>

    <style name="CardHeaders" parent="@style/AppTheme">
        <item name="android:textSize">@dimen/header_text_size</item>
        <item name="android:textColor">@color/colorCardHeaders</item>
    </style>

    <style name="CardDescriptions" parent="@style/AppTheme">
        <item name="android:textSize">@dimen/base_text_size</item>
        <item name="android:textColor">@color/darkGray</item>
    </style>

    <style name="SeeMore" parent="@style/AppTheme">
        <item name="android:textSize">@dimen/base_text_size</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <style name="PrimaryFlatButton" parent="@style/AppTheme">
        <item name="android:buttonStyle">
            @style/Widget.AppCompat.Button.Borderless.Colored
        </item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
